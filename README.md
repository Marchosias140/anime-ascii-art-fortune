Anime ascii art for the Fortune app.

sudo dnf install fortune

Copy the files to the corresponding directory

sudo cp anime anime.dat /usr/share/games/fortune

Then, use the following command in order to check if it worked:

fortune -f

If you want to see only quotes from the anime file, use this command:

fortune anime
